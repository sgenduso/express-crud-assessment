extends layout

block content
  p
    a(href='/articles/new') Add New Article
  for article in articles
    div.this-article-preview(style='background-image: url(article.url)')
      div.this-header
        h2
          a(href='/articles/'+article._id)=article.title
      div.this-excerpt
        =article.excerpt
